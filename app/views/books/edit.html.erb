<h1>Editing Book</h1>

<%= form_with model:@book, url:"/bookers/#{@book.id}", local:true do |f| %>

<h4>Tilte</h4>
<%= f.text_field :title %>

<h4>Body</h4>
<%= f.text_area :body %>

<%= f.submit 'Update Book' %>

<%= link_to "Show", book_path(@book) %>
<h4>|</h4>
<%= link_to "Back", books_path %>

<% end %>
